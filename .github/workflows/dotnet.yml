---
name: RocketAPIWorkFlow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  Deploy:
    runs-on:
      - ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: USNYMEQARKTMX2
        username: USHSI\VITHEYA.MONIKHA
        password: Passwordforhs@9595
        port: 22
        script: whoami
